@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;

@{
    var content = (ContentModels.ReviewsSection)Model.Content;
}

<section class="py-24 bg-white-100">
    <div class="container mx-auto flex items-center gap-12">

        
        <div class="w-1/2 flex flex-col gap-6">

            @if (!string.IsNullOrWhiteSpace(content.OverHead))
            {
                <p class="uppercase text-lg font-bold tracking-widest text-secondary">@content.OverHead</p>
            }           
           
            @if (!string.IsNullOrWhiteSpace(content.Text))
            {
                <h2 class="text-primary font-bold text-[1.875rem]">@content.Text</h2>
            }

            @if (!string.IsNullOrWhiteSpace(content.Text2))
            {
                <p class="text-gray-600 leading-relaxed text-lg italic">@content.Text2</p>
            }

            
            @if (content.Stars != null)
            {
                <div>
                    <img src="@content.Stars.Url()" alt="rating" class="h-5" />
                </div>
            }

           
            <div class="mt-2">
                @if (!string.IsNullOrWhiteSpace(content.Signature))
                {
                    <p class="font-normal text-gray-900">@content.Signature</p>
                }

                @if (!string.IsNullOrWhiteSpace(content.Bank))
                {
                    <p class="text-primary font-normal italic">@content.Bank</p>
                }
            </div>

        </div>

       
        <div class="w-1/2">
            @if (content.Image != null)
            {
                <img src="@content.Image.Url()"
                     alt=""
                     class="w-full h-[500px] object-cover rounded-lg shadow-sm" />
            }
        </div>

    </div>
</section>
